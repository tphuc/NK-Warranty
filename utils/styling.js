export const borderTopRadius = (num) => ({
    borderTopStartRadius: num,
    borderTopLeftRadius: num,
    borderTopRightRadius: num,
    borderTopEndRadius: num
})

export const borderBottomRadius = (num) => ({
    borderBottomStartRadius: num,
    borderBottomLeftRadius: num,
    borderBottomRightRadius: num,
    borderBottomEndRadius: num
})
